/target
/Derived
/.beads/*.db-shm
/.beads/*.db-wal

*.tif

*.prj

data/natural_earth/

data/srtm_gl1/

data/GEBCO/

data/

.DS_Store

# Local app state — settings (contains API keys), event DB, legacy key file.
# Normally written next to the executable, but never let one slip in from here.
.1kee_*

# Gruve integration kit — tooling (agent binary, zip). The Rust SDK is vendored
# into crates/gruve_sdk and the JS SDK into the desktop crate's web assets, so the
# build does not depend on this folder staying around.
/gruve-kit/
/gruve-kit.zip

