# 1kEE — Claude Code Guidelines

## Git workflow
- Commit directly to the current branch. Do NOT use git worktrees.
- The remote uses SSH (`git@github.com:MLTQ/1kEE.git`). Do not switch it to HTTPS.
- This is a solo project — commit to `master` directly, no PR workflow needed.
